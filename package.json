{
  "name": "orderrunner",
  "version": "1.0.10",
  "main": "dist/electron/main.js",
  "scripts": {
    "build:css": "npx tailwindcss -i ./styles.css -o ./dist/ui/output.css --minify",
    "build:render": "tsc -p tsconfig.render.json",
    "build:main": "tsc -p tsconfig.json",
    "copy:ui": "cpx \"ui/**/*.{html,css}\" dist/ui",
    "build": "npm run build:css && npm run build:main && npm run build:render && npm run copy:ui",
    "start": "npm run build && electron .",
    "install:browsers": "set PLAYWRIGHT_BROWSERS_PATH=0&& npx playwright install chromium firefox webkit",
    "pack": "npm run build && npm run install:browsers && electron-builder --dir",
    "dist": "npm run build && npm run install:browsers && electron-builder"
  },
  "build": {
    "appId": "com.orderrunner.app",
    "productName": "OrderRunner",
    "directories": {
      "output": "dist_installer"
    },
    "extraResources": [
      {
        "from": "node_modules/playwright-core/.local-browsers",
        "to": "playwright-browsers"
      }
    ],
    "files": [
      "dist/**/*",
      "node_modules/playwright-core/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico",
      "artifactName": "OrderRunner-Setup-${version}.${ext}"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "hdukonline",
        "repo": "orderrunner"
      }
    ]
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "autoprefixer": "^10.4.23",
    "concurrently": "^8.x",
    "cpx": "^1.5.0",
    "electron": "^26.x",
    "electron-builder": "^26.8.1",
    "nodemon": "^3.1.11",
    "playwright": "^1.59.1",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.x"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "@playwright/test": "^1.58.1",
    "playwright-core": "^1.x.x",
    "postcss-cli": "^11.0.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
